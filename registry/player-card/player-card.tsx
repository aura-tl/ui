import*as React from'react';import type{AuraPlayerCardModel,AuraPlayerCardState}from'@/lib/aura/player-card-types';import'./player-card.css';export function AuraPlayerCard({state,className=''}:{state:AuraPlayerCardState;className?:string}){return state.model?<AuraPlayerCardView model={state.model} className={className}/>:<article className={`aura-player-passport ${className}`}><p className="aura-player-passport__empty">{state.error||'Player unavailable.'}</p></article>}
export function AuraPlayerCardView({model,className=''}:{model:AuraPlayerCardModel;className?:string}){const initials=model.name.split(/\s+/).map(part=>part[0]).join('').slice(0,2);return <article className={`aura-player-passport ${className}`.trim()}><div className="aura-player-passport__portrait">{model.headshot?<img src={model.headshot} alt=""/>:<span>{initials}</span>}<small>{model.team}</small></div><div className="aura-player-passport__body"><header><span>{model.position}{model.number?` · #${model.number}`:''}</span><i>{model.status||'Active'}</i></header><h3>{model.name}</h3>{model.facts.length?<dl>{model.facts.map(fact=><div key={fact.label}><dt>{fact.label}</dt><dd>{fact.value}</dd></div>)}</dl>:null}</div></article>}
