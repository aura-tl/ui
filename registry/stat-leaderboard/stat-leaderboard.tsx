'use client';
import type { AuraAnalyticsAxisKey, AuraAnalyticsPoint, AuraLeagueLandscape } from '@/lib/aura/league-analytics-types';
import { formatAuraAnalyticsValue } from '@/lib/aura/league-analytics-types';
export function AuraStatLeaderboard({model,axis='y',limit=5,title}:{model:AuraLeagueLandscape;axis?:AuraAnalyticsAxisKey;limit?:number;title?:string}){const rows=[...model.points].sort((a,b)=>b[axis]-a[axis]||a.name.localeCompare(b.name)).slice(0,Math.max(1,Math.min(20,limit)));const metric=model.axes[axis];const complete=model.sample.completeEntities===model.sample.returnedEntities;return <section className="aura-stat-leaderboard"><header><div><small>{complete?'League leaders':'Top returned results'}</small><h2>{title||`Top ${rows.length} · ${metric.label}`}</h2></div><span>{model.sample.returnedEntities} {model.entityType}s · {model.sample.aggregation}</span></header><ol>{rows.map((point,index)=><li key={point.id}><b>{String(index+1).padStart(2,'0')}</b><span><strong>{point.name}</strong><small>{identity(point,model.entityType)}</small></span><i aria-hidden="true"><em style={{width:`${Math.max(4,point.percentile[axis]*100)}%`}}/></i><div><strong>{formatAuraAnalyticsValue(point[axis],metric)}</strong><small>p{Math.round(point.percentile[axis]*100)}</small></div></li>)}</ol><footer><span>{model.sample.games} games</span><span>{complete?'All matching results':`${model.sample.returnedEntities} of ${model.sample.completeEntities} matching results shown`}</span></footer></section>}
function identity(point:AuraAnalyticsPoint,type:AuraLeagueLandscape['entityType']){return [point.team,point.position].filter(Boolean).join(' · ')||type;}
