{
  "id": "aura.fantasy-draft-board.react-next",
  "component": "fantasy-draft-board",
  "dataPlane": "aura-only",
  "deliveryMode": "fetch",
  "dataManifest": {
    "version": 1,
    "component": "fantasy-draft-board",
    "delivery": { "primary": "fetch", "snapshot": ["nfl-draft"] },
    "requests": [
      { "id": "nfl-draft", "method": "GET", "path": "/api/stats/nfl/draft", "view": "page", "fields": ["items[].entityId", "items[].playerId", "items[].teamId", "items[].name", "items[].position", "items[].projection", "items[].draft", "items[].fantasy"], "parameters": ["season=2026", "scoring=ppr", "limit=250"] }
    ],
    "relationships": ["Projection and draft rank share one Player or Team entity id."],
    "unavailable": ["Only retained 2026 entities render.", "Missing positions, teams, portraits, ranks, and projection depth are never invented."]
  },
  "objects": ["Player", "ResearchResult"],
  "instructions": ["Render the real retained fixture first.", "Set server-only Aura credentials and bind one memoized createAuraLiveScoreboardClient() to useAuraFantasyDraft().", "Scoring changes are recomputed from retained stats; the draft queue is local selection state only."],
  "customization": { "howTo": "Edit scoring, filters, sorting, and roster behavior directly in the copied source.", "fieldCatalogObjects": ["nfl-draft"], "docs": "docs/component-data-customization.md" }
}
